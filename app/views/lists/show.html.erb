<header class="primary">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-10 col-sm-offset-1">
        <h1><%= @list.name %></h1>
        <%= link_to "Add a new bookmark", new_list_bookmark_path(@list), class: 'btn btn-primary' %>
        <%= link_to "See all movies", lists_path, class: 'btn btn-primary' %>
        
      </div>
    </div>
  </div>
</header>
